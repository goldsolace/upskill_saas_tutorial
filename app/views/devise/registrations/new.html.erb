<div class="container">
  <div class="row">
    <div class="col-md-4 col-md-offset-4">
        
        <% if params[:plan] == '2' %>
          <%= render "pro_form" %>
        <% else %>
          <%= render "basic_form" %>
        <% end %>
        
    </div>
  </div>
</div>
